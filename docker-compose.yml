version: "3.8"

services:
  # Student Dashboard Application with redpwn/jail
  app:
    build: .
    container_name: student-dashboard-app
    ports:
      - "5000:5000"
    environment:
      - FLAG=FlagY{test_flag_for_development}
    privileged: true
    restart: unless-stopped
